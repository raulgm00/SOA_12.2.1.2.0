package org.bcie.fenix.common.model.vo;

import java.math.BigDecimal;

import oracle.adf.share.logging.ADFLogger;

import oracle.jbo.Row;
import oracle.jbo.server.ViewObjectImpl;

import org.bcie.fenix.common.model.vo.common.AgregarLineaCreditoVO;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Fri Mar 18 13:56:20 CST 2016
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class AgregarLineaCreditoVOImpl extends ViewObjectImpl implements AgregarLineaCreditoVO {
    
    private static ADFLogger logger = null;
    
    /**
     * This is the default constructor (do not remove).
     */
    public AgregarLineaCreditoVOImpl() {
        if (logger == null) {
            logger = ADFLogger.createADFLogger(this.getClass());
        }
    }
    
    public void crearRowAgregarLineaCredito(){
        logger.log(ADFLogger.TRACE, "Into crearRowAgregarLineaCredito.");
            this.clearCache();
            Row row = this.createRow();
            row.setAttribute("Id", Long.valueOf(1));
            this.insertRowAtRangeIndex(0, row); 
            this.setCurrentRow(row); // Asignamos nuevo row como current
    }
    
    public void crearUnicoRowAgregarLineaCredito(){
        logger.log(ADFLogger.TRACE, "Into crearUnicoRowAgregarLineaCredito.");
        if (getEstimatedRowCount() == 0) {
            Row row = this.createRow();
            row.setAttribute("Id", Long.valueOf(1));
            
            this.insertRow(row);
            this.setCurrentRow(row);
        }
    }
    
    public void crearAsignarRowLineaCredito(Long id,
                                            String numeroLineaCredito,
                                            String descripcion,
                                            BigDecimal monto){
        
        AgregarLineaCreditoVORowImpl row = null;
        
        row = (AgregarLineaCreditoVORowImpl)createRow();
        
        row.setId(new Long(-1));
        row.setNumeroLineaCredito(numeroLineaCredito);
        row.setDescripcionLinea(descripcion);
        row.setMontoLinea(monto);
        
        row.setNewRowState(Row.STATUS_INITIALIZED);
        insertRow(row);
        setCurrentRow(row);
    }
}

