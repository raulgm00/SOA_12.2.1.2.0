-- DROP TABLE FENIX.TCA_TIPO_CUESTIONARIO PURGE;
    -- CREATE TABLE "FENIX"."TCA_TIPO_CUESTIONARIO" 
    -- (	
        -- "ID" NUMBER(5,0) NOT NULL ENABLE, 
        -- "DESCRIPCION" VARCHAR2(256 BYTE) COLLATE "USING_NLS_COMP" NOT NULL ENABLE, 
        -- "DESCRIPCION_CORTA" VARCHAR2(64 BYTE) COLLATE "USING_NLS_COMP" NOT NULL ENABLE, 
        -- "BAN_ESTATUS" NUMBER(1,0) DEFAULT 1 NOT NULL ENABLE, 
        -- "FECHA_REGISTRO" DATE DEFAULT CURRENT_DATE NOT NULL ENABLE, 
        -- "COD_EXTERNO" VARCHAR2(32 BYTE) COLLATE "USING_NLS_COMP", 
        -- CONSTRAINT "TCA_TIPO_CUESTIONARIO_PK" PRIMARY KEY ("ID")
    -- );