package org.bcie.fenix.common.model.vo;

import oracle.adf.share.logging.ADFLogger;

import oracle.jbo.Row;
import oracle.jbo.server.ViewObjectImpl;

import org.bcie.fenix.common.model.vo.common.TccSeleccionarVO;

// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Thu Feb 25 13:37:20 CST 2016
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class TccSeleccionarVOImpl extends ViewObjectImpl implements TccSeleccionarVO {
    
    private static ADFLogger logger = null;
    
    /**
     * This is the default constructor (do not remove).
     */
    public TccSeleccionarVOImpl() {
        
        if (logger == null) {
            logger = ADFLogger.createADFLogger(this.getClass());
        }
    }
    
    public void inicializarSeleccionarVO(Long idOperacion){
        logger.warning("Dentro de inicializarSeleccionarVO");

        this.crearRowSeleccionar();
        this.getCurrentRow().setAttribute("IdOperacion", idOperacion);
        
        // Execute query de los LOVs del row (por si se eliminó un registro)
        TccSeleccionarVORowImpl currentRow = (TccSeleccionarVORowImpl)this.getCurrentRow();
        currentRow.getTccSeleccionarTerminoLOV().executeQuery();
        logger.warning("Cantidad de registro de termino :"+currentRow.getTccSeleccionarTerminoLOV().getEstimatedRowCount());
        currentRow.getTccSeleccionarCondicionLOV().executeQuery();
        currentRow.getTccSeleccionarComisionLOV().executeQuery();
        logger.warning("Fuera de inicializarSeleccionarVO");
    }
    
    private void crearRowSeleccionar(){
        logger.log(ADFLogger.TRACE, "Inside crearRowSeleccionar.");
        
        this.clearCache(); // Limpiamos VO
        Row row = this.createRow();
        row.setAttribute("Id", Integer.valueOf(1));
        
        this.insertRowAtRangeIndex(0, row); // Insertamos row en VO
        this.setCurrentRow(row); // Asignamos nuevo row como current
    }
}

